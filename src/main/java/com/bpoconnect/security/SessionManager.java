package com.bpoconnect.security;

import org.springframework.stereotype.Component;
@Component public class SessionManager { public void createSession(String u){} public boolean validateSession(String t){return true;} public void invalidateSession(){} }
